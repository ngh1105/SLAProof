# SLAProof Vendor Escalation Template

Subject: SLA incident review for `<provider>` on `<chain>`

Hello `<vendor team>`,

We are reviewing an incident affecting `<endpoint label>` during the following
UTC window:

- Start: `<start utc>`
- End: `<end utc>`
- Chain: `<chain>`
- Endpoint: `<endpoint label>`

Our SLAProof receipt currently reports:

- Decision: `<breach | no_breach | inconclusive | needs_more_evidence>`
- Confidence: `<confidence>%`
- Receipt hash: `<receipt hash>`
- Relevant clause: `<clause or none>`

Evidence considered:

1. `<evidence id>`: `<finding>`
2. `<evidence id>`: `<finding>`
3. `<evidence id>`: `<finding>`

Requested next step:

`<recommended next action>`

Please confirm whether your incident records match the attached evidence and
whether the SLA credit rule applies.

Important note: the receipt is an evidence aid for operational review. It is not
a legal determination.

